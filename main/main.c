// main.c — Stub. With framework=arduino, the Arduino runtime
// provides its own app_main (in cores/esp32/main.cpp), so we
// must NOT define our own. All initialization happens in
// setup() in sketch.cpp.

#include <stdio.h>
#include "sdkconfig.h"